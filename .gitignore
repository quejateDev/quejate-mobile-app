node_modules/
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
npm-debug.*
yarn-debug.*
yarn-error.*

.env
.env.local
.env.local.bak

android/

ai/

# Internal diagnostics screen — kept locally for testing, not shipped
src/features/debug/

# Firebase Android config — contains an API key. Distributed via EAS file env
# var GOOGLE_SERVICES_JSON; keep a local copy at repo root for `expo run:android`.
google-services.json

.DS_Store